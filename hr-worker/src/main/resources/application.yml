server:
  port: 8001
spring:
  application:
    name: rh-worker
  datasource:
    password: 'admin1234'
    url: jdbc:h2:mem:bancorh
    username: sa
  h2:
    console:
      enabled: true
      path: /h2-console
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    generate-ddl: true
    hibernate.ddl-auto: none
  
